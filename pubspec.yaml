name: izipay_payment
description: "A Flutter package to tokenize payment methods with Izipay. Integrates the native SDK to facilitate its use and improve the payment experience."
version: 1.0.0
maintainer: Carlos España (@creegplay)
homepage: https://github.com/carlinsk8/izipay_payment
repository: https://github.com/carlinsk8/izipay_payment
issue_tracker: https://github.com/carlinsk8/izipay_payment/issues
documentation: https://github.com/carlinsk8/izipay_payment/blob/master/README.md

environment:
  sdk: ^3.5.2
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.izipay.payment.izipay_payment
        pluginClass: IzipayPaymentPlugin
      ios:
        pluginClass: IzipayPaymentPlugin